package kmp.shared.base.preferences

/**
 * Shared keys for [com.russhwolf.settings.Settings].
 * Keep in sync across modules that read/write the same values.
 */
object AppSettingsKeys {
    const val NASA_API_KEY = "nasa_api_key"
}
